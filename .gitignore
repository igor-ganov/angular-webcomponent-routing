# dependencies & build output
node_modules/
dist/
.angular/

# editor / misc
*.log
.DS_Store
.setting/

# --- workspace packages are independent git repos ---
# Locally they are plain directories so `bun` workspaces can link them.
# `publish.ps1` converts them into git submodules once their remotes exist.
/router-lib/
/web-component/
/host-app/
